# Copy this file to .env.local and fill in values as needed.

# Optional: Require this token in API mutations (POST/PUT/PATCH/DELETE)
# When set, clients must send header: x-admin-token: <ADMIN_TOKEN>
ADMIN_TOKEN=$ecret$ecret$66

# Used by the RSS feed to generate absolute links.
# In dev you can leave this as default; set to your production URL when deploying.
SITE_URL=http://localhost:3000

